services:
  ${rootName}-ocsp:
    build: ./ocsp-root/
    init: true
    networks:
      proxy-internal:
    volumes:
      - ${rootName}-ocsp-data:/etc/ocsp
    restart: unless-stopped
  
  proxy:
    volumes:
      - ./${proxyFile}:/etc/nginx/conf.d/${rootName}.http

